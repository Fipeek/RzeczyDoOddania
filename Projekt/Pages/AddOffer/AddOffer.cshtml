@page
@model Projekt.Pages.AddOffer.AddOfferModel
@{
}
<div>
   <form method="post" enctype="multipart/form-data">
       <label>Name</label>
        <input asp-for="Offer.Name" />
       <label>Location</label>
        <input asp-for="Offer.Location" />
       <label>Description</label>
        <input asp-for="Offer.Description" />
        <input asp-for="Offer.Date" type="date" />
        <input  asp-for="UploadedFile" />

        <select asp-for="Category1">
            @foreach(var category in @Model.Categories.Categories)
            {
            <option>@category.Name</option>
          
                
            }
        </select>        
        <select asp-for="Category2">
            @foreach(var category in @Model.Categories.Categories)
            {
            <option>@category.Name</option>
           
                
            }
        </select>
        <button type="submit">Zatwierdz</button>
   </form>
</div>